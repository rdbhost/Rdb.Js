/*
 *  Plugin for Rdbhost, providing friendly functions for common utils
 *
 *
 */


function emailWebmaster(opt) {

    /*
     *  to,
     *  from,
     *  subject,
     *  body,
     *  htmlBody,
     *  attachmentName,
     *  attachmentBody
     *
     *  callback,
     *  errback
     */

}



function setupEmail(opts) {

    /*
     * service,
     * apikey,
     * webmasterEmail,
     * accountEmail,
     * callback
     * errback
     *
     */

}



function chargeCard(opts) {

    /*
     *  cardNum,
     *  ccv,
     *  Name,
     *  Amount,
     *  acctId,
     *
     *  callback
     *  errback
     */

}



function setupCharge(opts) {

    /*
     *  service,
     *  apikey,
     *
     *  callback,
     *  errback
     *
     */


}


function superLogin(opts) {

    /*
     * email,
     * password,
     * acctId,
     *
     * callback,
     * errback
     */
}


function superPostData(opts) {

    /*
     * same as postData
     *   if no authcode, logs in to get super authcode
     *   sets timeout to clear authcode
     */


}


function superPostFormData(formid, opts) {

    /*
     * same as postFormData
     */


}


function preauthPostData(opts) {

    /*
     * same as postData
     *   if get error...
     */
}


function preauthPostFormData(opts) {

    /*
     * same as postData
     *   if get error...
     */


}